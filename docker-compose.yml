services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: lode
      POSTGRES_PASSWORD: lode
      POSTGRES_DB: lode
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: